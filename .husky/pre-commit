# Pre-commit quality checks (tests disabled)
echo "🔍 Running pre-commit checks..."

# Check for linting errors  
npm run lint 2>/dev/null || echo "⚠️  No lint script found"

echo "✅ Pre-commit checks complete"
